{
  "name": "aether-ui",
  "version": "0.1.0",
  "description": "Aether UI CLI — Install components from the void",
  "type": "module",
  "bin": {
    "aether": "./dist/index.js"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts",
    "dev": "tsup src/index.ts --format esm --watch"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.5.0"
  }
}
